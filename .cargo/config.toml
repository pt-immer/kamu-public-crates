# Workspace-wide cargo configuration.
#
# Build profiles live in the root Cargo.toml ([profile.release]); this file
# only carries target-specific rustflags so the two do not conflict.

[target.'cfg(target_arch = "x86_64")']
rustflags = ["-C", "target-cpu=x86-64-v2"]
